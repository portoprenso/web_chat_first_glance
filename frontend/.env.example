# Optional overrides.
# Vite dev and test default to the local backend on port 3000.
# Production-style runs default to the current browser origin.
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000/api/ws
