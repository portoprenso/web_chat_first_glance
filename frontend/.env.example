# Optional overrides. By default the frontend uses the current browser origin,
# which works with the Vite dev proxy and the Docker nginx proxy.
VITE_API_URL=http://localhost:5173
VITE_WS_URL=ws://localhost:5173/api/ws
