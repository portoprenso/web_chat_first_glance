# Docker Compose host directories for persistent local data.
# You can keep the defaults, use relative paths, or switch to absolute paths.
POSTGRES_DATA_DIR=./.docker-data/postgres
BACKEND_STORAGE_HOST_DIR=./.docker-data/storage
